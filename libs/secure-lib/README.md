# secure-lib

Utility functions library for secure operations.

## Usage

Import functions from this library in your apps:

```typescript
import { someFunction } from 'secure-lib';
```

## Development

- `npm run build` - Build the library
- `npm run dev` - Build in watch mode
- `npm run lint` - Lint the code
- `npm run typecheck` - Type check without emitting files

